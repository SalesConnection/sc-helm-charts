{{- if .Values.enabled }}
apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "qr-generator.fullname" . }}-test-connection"
  labels:
    {{- include "qr-generator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: echo
      image: busybox
      command: ['echo']
      args: ['{{ include "qr-generator.fullname" . }}']
  restartPolicy: Never
{{- end }}
