apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "caction.fullname" . }}-test-connection"
  labels:
    {{- include "caction.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "caction.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
