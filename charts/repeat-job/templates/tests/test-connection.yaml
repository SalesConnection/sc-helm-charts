apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "repeat-job.fullname" . }}-test-connection"
  labels:
    {{- include "repeat-job.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "repeat-job.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
